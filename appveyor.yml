image:
  - Visual Studio 2019
  - Ubuntu1804

install:
  - git submodule update --init --recursive

build:
  verbosity: detailed

build_script:
  - cmake -S . -B build
  - cd build
  - cmake --build .

test_script:
  - cd test
  - ctest -C Release --output-on-failure